language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-react-slick' ]; then cd .. && eval "mv $currentfolder generator-react-slick" && cd generator-react-slick; fi
